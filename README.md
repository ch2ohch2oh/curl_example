# Examples on libcurl

## Basic facts
- libcurl is written in C and support multiple network protocols.

## Environment setup
On ubuntu

    $ sudo apt install libcurl4-openssl-dev

To test whether curl dev is successfully installed

    $ curl-config

